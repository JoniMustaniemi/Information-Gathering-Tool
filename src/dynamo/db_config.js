const config = {
  aws_table_name: 'Users',
  aws_local_config: {
    // Provide details for local configuration
  },
  aws_remote_config: {
    accessKeyId: '',
    secretAccessKey: '',
    region: 'eu-west-2',
  }
};

export default config;